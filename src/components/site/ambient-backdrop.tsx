// Ambient backdrop is no longer needed with the light blue-shell design system.
// Keeping the export to avoid import errors across the codebase.
export function AmbientBackdrop(_props: { intensity?: 'subtle' | 'medium' | 'strong'; className?: string }) {
  return null
}
